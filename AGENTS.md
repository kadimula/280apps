This repo is the source code for 280apps.com, a platform which allows humans to easily and securely share personal apps. This repo holds the 280 CLI, backend code, and contracts.

280apps is built agent-first, with CLI conforming to the [axi.md](https://axi.md/) format.

## Who is 280 best for:

Non technical professionals building internal tools with coding agents. Basic CRUD apps with integrations, a database, file storage, etc. Their team already lives in a workplace identity system (Microsoft, Google). They cannot and should not debug deploys, configure auth, or reason about security.

280 owns the full deploy, identity, sharing, permissions pieces. Reducing the headache of a non-technical professional who is trying to build useful software for themselves and their team

## Agent-facing docs (source of truth)

Owned by the backend (`packages/backend/src/docs.ts`), served as markdown and JSON under `/v1/docs/*` via `docsRoutes()` (one-line mount in `src/api.ts`).

- setup.md (`packages/backend/src/docs/setup.md`): the entry prompt agents fetch ("Fetch https://www.280apps.com/setup.md and push"), then run `npx -y two80@latest push` (Node 20+). Section 1 embeds the support matrix inline as two small tables (supported stacks / not supported); there is no separate platform-support doc or route anymore.
- Platform support: the structured matrix in `packages/backend/src/docs.ts` serves `/v1/docs/capabilities` for the landing page `/docs` route. The agent-facing matrix now lives inline in setup.md section 1 (the standalone `platform-support.md` doc and its `/v1/docs/platform-support.md` route were removed).
- the-280-way (`the280WayMarkdown()`): the build reference agents follow; one blessed answer per need. Next.js App Router + Tailwind/shadcn; provisioned Postgres via Drizzle + serverless driver; R2 for files; identity read from `x-280-user/roles/actions` headers (never write auth); `280.json` manifest declares features, actions, secrets, crons; the Never list (no auth libs, no native modules, no fs writes, no websockets, no long running processes); loop: build with `two80 dev`, push, fix what push says, verify URL.

Product and tech vision (direction, not shipped): any app deploys (push instructs, agent converges); auth comes built in (apps hold zero login code, 280 is the front door); sharing works like Docs (grants by email/domain/link, per feature roles, custom actions, instant apply, "view as"). Locked tech decisions: Cloudflare Workers as opinionated harness; edge front door injects signed identity headers and blocks writes for readers; rolled OIDC (Google + Microsoft); per app Postgres + R2; server side builds + boot check with errors translated into agent instructions; `two80 dev` proxy injects fake identity locally. Shipped today: push, device login, deploy to Workers.

## Shipped flow (file that owns each step)

- Agent fetches setup.md (`packages/backend/src/docs/setup.md`), then runs `npx -y two80@latest push` (Node 20+).
- CLI = `packages/cli` (npm and bin `two80`). `src/push.ts`: sync, upload blobs, poll; auto-inits and bundles (`src/bundle/`). `src/app.ts` `run(env, deps)` is pure, all side effects injected via `Deps`; `src/bin.ts` is the only composition root. Stdout is TOON, proven by golden fixtures `packages/cli/testdata/*.toon` (regenerate with `UPDATE_FIXTURES=1`). `.280/config.json` (committed) binds dir to app; `~/.280/credentials` holds the machine token. Releasing: `packages/cli/RELEASING.md` (bump `package.json`, push a `cli-v*` tag, CI publishes to npm via OIDC).
- Device login: `packages/cli/src/login.ts` never blocks; prints link + code, exits `authorization_pending`; agent relays, never opens the link. User approves in the browser (their one action); re-push redeems the token.
- Backend (control plane) = `packages/backend` (npm `@280/backend`): transport `packages/backend/src/api.ts`; behavior `src/deploysvc.ts`; Postgres store `src/store/`; blobs `src/blobstore/`; container deployment coordination `src/activator.ts`; live substrate `src/runtime/container/` (apps run UNCHANGED as Cloudflare Containers). The transport-agnostic Hono app runs on one host today: the Node entry `src/main.ts` (direct `pg.Pool`, `S3BlobStore` over R2's S3 API, inline `ContainerDeploymentCoordinator`, interval sweep), the Railway deploy target (`packages/backend/Dockerfile`, env documented in `.env.example`). The dead Cloudflare Workers entrypoint (`src/worker.ts`) and its per-app `AppActivator` Durable Object shim were removed once Node went live; `src/activator.ts` holds the live `ContainerDeploymentCoordinator`. The coordinator hands each build context to a `ContainerBuilder` (`src/runtime/container/container.ts`), created by `buildContainerServices` (`deps.ts`): `DepotBuilder` (`depot-builder.ts`, the sole build home — daemonless remote BuildKit, one `depot build --push` to `registry.cloudflare.com`, needs `DEPOT_TOKEN`/`CLOUDFLARE_*`) is the only build path (the retired HttpBuilder/self-hosted-Docker-host and `TWO80_BUILD_*` config were deleted). The node builder `DepotBuilder` extends `CloudflareContainerDeployment` (`cloudflare-container-deployment.ts`), which owns the shared context materialize, teardown, exec discipline, and the roll: `wrangler deploy --containers-rollout immediate` against a generated config whose container `image` is the pre-built registry ref, so no Docker runs on the roll (the old `wrangler containers apply` never existed in wrangler 4.116 and silently no-oped — report §7). Adding a build home changes only the builder created by `buildContainerServices`; nothing above the seam changes. Depot's per-app project + the harness worker `main` (`APP_WORKER_ENTRYPOINT`) are wired live in the gated Railway follow-up. `platform/appcontainer/` holds `App280Container` (locked defaults: `enableInternet=false`, `interceptHttps=true`, buildpack-injected CA entrypoint). The CLI `bundle/container.ts` buildpack turns a repo into that build context (Next.js Dockerfile generated, or a user root `Dockerfile` as the escape hatch); the WfP+OpenNext path is retired. The legacy edge dispatcher (`platform/dispatcher/`) and its `apps`/`apps-development` dispatch namespaces were deleted in the Phase 3 cutover (PR fm/280-wfp-deletion); `*.280apps.run` is now served container-only by per-app Workers fronting the identity gateway (`packages/gateway`). Last blob landing triggers activation, no separate verb: the request settles the deploy (`deploysvc` settle) through the inline `ContainerDeploymentCoordinator` (`src/activator.ts`), which claims and runs container deployment serialized per app by a promise chain, then the CLI polls Status to a terminal state. Edge can lag ~1 min. Store SQL is schema-qualified through one `t(name)` helper (no `search_path` dependence, for transaction-mode pooling); the idempotent DDL lives once in `src/store/migrations.ts`, applied both on boot (`open()`) and by the CI-only standalone runner `src/migrate.ts` (`pnpm --filter @280/backend migrate`). Migrations are never HTTP-reachable or run at runtime by design. The two-tier permission model (design §5.4) is the flat `grants` table behind the same `Store` seam: `app_role` (owner|admin|editor|viewer) governs the app as an object, `feature_role` is builder-defined per app (custom actions fold into it via `can()`), keyed on (app_id, principal). Phase 4 wired it end to end (see the permissions bullet below).
- Contracts seam: `packages/contracts` (`deploy`, `auth`, `version` types, zod, Port), consumed by CLI and backend. The CLI bundles it (tsup `noExternal`) so npx works. Adapter subpaths (`@280/contracts/deploy/{http,fake}`, `.../auth/http`) via `exports`. `SyncResult.missing` serializes as `null` when empty.
- Agent integrations: `packages/cli/src/setup/`. `two80 setup` JSON-merges (never overwrites) a SessionStart hook into Claude Code, Codex, OpenCode and installs the skill. `packages/cli/skill/SKILL.md` is generated from the home view: `pnpm --filter two80 skill:gen`; CI runs `two80 setup --check` to fail on drift.
- Outbound network boundary = fixed SDK API only. `App280Container` keeps `enableInternet=false` and `interceptHttps=true`; `platform/appcontainer/src/container.js` reads the platform-owned `TWO80_SDK_API_ORIGIN`, sets that exact hostname as its sole `allowedHosts` entry, and injects the origin into the container as `TWO80_API`. Cloudflare's `ContainerProxy` returns HTTP 520 for every other host. The former `@280/egress` credential handler and per-app `280.json` policy were removed; the CLI and backend reject a non-empty legacy `egress` field. `packages/contracts` temporarily retains the empty wire shape for protocol compatibility. `platform/appcontainer/scripts/bundle-smoke.sh` proves the production bundle contains the fixed boundary and no retired handler.
- App-management dashboard = `packages/dashboard` (Next 16 / React 19, npm not pnpm). The human-facing surface for the two-tier model: dashboard (app list + per-app preview/share/view-as), login, device `activate`, and `docs`. It is a standalone npm app with its own `package-lock.json` and is deliberately excluded from the pnpm workspace (`!packages/dashboard` in `pnpm-workspace.yaml`) so it never destabilizes the backend lockfile — build it from inside the dir (`npm ci && npm run build`), not via pnpm. All backend access goes through `lib/api.ts` `apiBase()`, which requires `TWO80_API` (no host baked in; self-hostable). Deploy wiring (Railway/console) is a separate follow-up, not yet in-repo.
- Permissions/manifest/SDK (phase 4) = the enforcement of the two-tier model end to end. The app's trust boundary in `280.json` covers `access`, feature `roles`, route gates, and config; outbound destinations are platform-owned rather than app-declared. The dashboard General-access override in `app_policies.access_override` wins durably over the manifest, and `public` serves no-session visitors with a gateway-signed anonymous identity. The gateway enforces access and route gates, mints the signed identity, and audits access. `@280/sdk` decodes the gateway-stamped `X-280-Identity` header and exposes `{user, can, scope}`; it does not re-verify the signature (the gateway verifies it and owns the container's sole ingress, so the app trusts the header). Its `sdkApiUrl` primitive only resolves `/v1/sdk/*` paths against the injected `TWO80_API`; backend capability endpoints and workload authorization are the next SDK layer.
- Config is app-readable data from the `config` block in `280.json`, parsed by `manifest280.ts`, validated by `validateConfig`, resolved by `ConfigDelivery`, baked into the plaintext `TWO80_CONFIG` Worker var, and expanded into `process.env` by `App280Container`. Credentials must not enter app config. The former app-declared credential delivery path is retired; provider credentials will live behind backend SDK capability endpoints. The existing secret storage seam remains until those endpoints own it. `TWO80_CONFIG` never carries a credential value.


## Code style

Avoid comments wherever possible; make the code self-explanatory through naming and structure instead. Prefer a clear function or variable name, a named constant, or a small restructure over a comment that explains what the code does. Add a comment (2 lines maximum) only when the code genuinely cannot be made self-explanatory: a non-obvious "why", a subtle invariant, or a workaround rationale. Never restate the code, narrate the next line, or leave section-divider banners or commented-out code. Load-bearing markers (license headers, tooling directives, `eslint-disable` pragmas, machine-read annotations) stay.

## Development environment

Dev mirrors prod on isolated infra: apps deploy container-only as per-app Workers serving at `*-development.280apps.run` (fronting the dev identity gateway at `auth-development.280apps.run`), and the CLI targets it with `TWO80_API=https://api-development.280apps.com`. See `docs/dev-environment.md` for the namespace/URL/CLI-targeting details and the Railway dev variable manifest. Every env var the backend and gateway read is documented in the committed `.env.example` (names + shapes only; secrets are placeholders).

## Local notes

Private/local notes and infra details live in `CLAUDE.local.md` (gitignored, not committed). Never put secrets (connection strings, keys, tokens, passwords) in this committed file; they belong in `CLAUDE.local.md` or a secrets manager.

## Maintaining this file

Keep this file for knowledge useful to almost every future agent session in this project.
Do not repeat what the codebase already shows; point to the authoritative file or command instead.
Prefer rewriting or pruning existing entries over appending new ones.
When updating this file, preserve this bar for all agents and keep entries concise.
