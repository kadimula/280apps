.claude/worktrees/
platform/data/
platform/dispatcher/node_modules/
.claude/
.agents/
.github/


# TS rewrite workspace (packages/*)
node_modules/
packages/*/dist/

# Local proof: the container build context the buildpack materializes (throwaway)
platform/appcontainer/proof-context/

# Local-only filesystem blob store (the Node host's dev fallback when TWO80_S3_* is unset)
data/

# Local-only: editor scratch + private notes
.lavish/
docs/*
!docs/dev-environment.md
*.local.md

# Local env / secrets
.env
.env.*
!.env.example

# Cloudflare Wrangler local cache
.wrangler/
**/.wrangler/

# e2e test in progress
tests/


# Personal agent tooling (sources live in gitignored .agents/)
skills-lock.json
