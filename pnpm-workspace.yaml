# TS rewrite workspace (plan §2). Covers packages/* ONLY.
# packages/dashboard is intentionally excluded: it is a standalone Next.js/npm
# app with its own package-lock and its own build, and must not be pulled into
# the backend workspace (same reason the old frontend/ app was kept out).
packages:
  - 'packages/*'
  - '!packages/dashboard'
